{
  "name": "@sansys/dss-mcp",
  "version": "1.0.0",
  "description": "MCP server for Design System Sansys — Read-Only Foundation (Phase 1)",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsup src/index.ts src/http-server.ts src/sse-server.ts --format esm --out-dir build",
    "dev": "tsup src/index.ts src/http-server.ts src/sse-server.ts --format esm --out-dir build --watch",
    "start": "node build/index.js",
    "start:http": "node build/http-server.js",
    "start:sse": "node build/sse-server.js",
    "start:sse:bg": "nohup node build/sse-server.js > /tmp/dss-mcp-sse.log 2>&1 & echo $!",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "content-type": "^1.0.5",
    "raw-body": "^3.0.2",
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
